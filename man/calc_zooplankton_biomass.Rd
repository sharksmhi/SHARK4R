% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zooplankton_biomass.R
\name{calc_zooplankton_biomass}
\alias{calc_zooplankton_biomass}
\title{Calculate zooplankton biomass from dry weight and abundance}
\usage{
calc_zooplankton_biomass(
  data,
  abundance_parameter = "Abundance",
  integrated_abundance_parameter = "Integrated abundance",
  dry_weight_parameter = "Dry weight (mean)",
  biomass_concentration_parameter = "Biomass concentration",
  integrated_biomass_parameter = "Integrated biomass",
  append = TRUE,
  drop_na_values = TRUE,
  keep_reference = FALSE
)
}
\arguments{
\item{data}{A data frame or tibble in SHARK zooplankton format. Must contain
the observation-key columns listed in the \emph{Observation key} section, plus
\code{parameter} and \code{value}.}

\item{abundance_parameter}{Character string giving the parameter name for
abundance in \code{ind/m3}. Defaults to \code{"Abundance"}.}

\item{integrated_abundance_parameter}{Character string giving the parameter
name for integrated abundance in \code{ind/m2}. Defaults to
\code{"Integrated abundance"}.}

\item{dry_weight_parameter}{Character string giving the parameter name for
per-individual dry weight in \code{ug}. Defaults to \code{"Dry weight (mean)"}.}

\item{biomass_concentration_parameter}{Character string used for calculated
biomass concentration rows. Defaults to \code{"Biomass concentration"}.}

\item{integrated_biomass_parameter}{Character string used for calculated
integrated biomass rows. Defaults to \code{"Integrated biomass"}.}

\item{append}{Logical. If \code{TRUE} (default), append calculated biomass rows
to \code{data}. If \code{FALSE}, return only the calculated biomass rows.}

\item{drop_na_values}{Logical. If \code{TRUE} (default), drop calculated rows
where biomass could not be calculated and \code{value} is \code{NA}.}

\item{keep_reference}{Logical. If \code{TRUE}, keep the dry-weight coefficient
and reference columns used to compute biomass in the output.}
}
\value{
A tibble. By default, the original data are returned with calculated
biomass rows appended. If \code{append = FALSE}, only the calculated rows are
returned.
}
\description{
Calculates zooplankton biomass by combining per-individual dry weight
(\code{"Dry weight (mean)"}, in \code{ug}) with abundance measurements in the same
observation. Two biomass parameters are produced:
}
\details{
\itemize{
\item \strong{Biomass concentration} (\code{mg/m3}) from \code{"Abundance"} rows
(\code{ind/m3}).
\item \strong{Integrated biomass} (\code{mg/m2}) from \code{"Integrated abundance"} rows
(\code{ind/m2}).
}

The conversion is \code{biomass = dry_weight_ug * abundance / 1000} so that the
result is expressed in \code{mg/m3} or \code{mg/m2}.
}
\section{Observation key}{

Dry-weight and abundance rows are matched one-to-one using the following
columns, which together identify a single zooplankton observation in SHARK:
\itemize{
\item \code{platform_code}
\item \code{station_name}
\item \code{sample_date}
\item \code{sample_time}
\item \code{sample_min_depth_m}
\item \code{sample_max_depth_m}
\item \code{aphia_id}
\item \code{sex_code}
\item \code{dev_stage_code}
\item \code{size_class}
}

One biomass row is produced per matching abundance row (no aggregation
across size classes or development stages). Abundance rows without a
matching dry-weight value yield \code{NA} biomass and are dropped by default.

If no \code{"Dry weight (mean)"} rows are present in \code{data}, the function calls
\code{\link[=calc_zooplankton_dry_weight]{calc_zooplankton_dry_weight()}} internally to compute them from
\code{"Length (mean)"} before calculating biomass.
}

\examples{
zoo <- dplyr::tibble(
  platform_code = "77SE",
  station_name = "ANHOLT E",
  sample_date = as.Date("2023-06-01"),
  sample_time = "10:00",
  sample_min_depth_m = 0,
  sample_max_depth_m = 30,
  aphia_id = 104251,
  sex_code = NA_character_,
  dev_stage_code = "AD",
  size_class = NA_character_,
  parameter = c("Length (mean)", "Abundance", "Integrated abundance"),
  value = c(800, 120, 3600),
  unit = c("um", "ind/m3", "ind/m2")
)

calc_zooplankton_biomass(zoo, append = FALSE)

}
\seealso{
\code{\link[=calc_zooplankton_dry_weight]{calc_zooplankton_dry_weight()}}
}
