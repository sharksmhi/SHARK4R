# Uses data from national marine monitoring for the last 5 years to identify outliers

**\[deprecated\]** This function is deprecated and has been replaced by
[`check_outliers()`](https://sharksmhi.github.io/SHARK4R/reference/check_outliers.md).

Ranges and IQR (interquantile range) for specific parameters is adapted
to each datatype

## Usage

``` r
check_zooplankton_length_mean(data)
```

## Arguments

- data:

  for tibble be be checked

## Value

tibble of data with outliers
