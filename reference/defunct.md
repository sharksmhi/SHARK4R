# Defunct functions

**\[defunct\]**

These functions were deprecated before being made defunct. If there's a
known replacement, calling the function will tell you about it.

## Usage

``` r
get_shark_table(...)

check_data_version(...)

update_data(...)

download_sharkdata(...)

load_dataset_names(...)

load_sharkdata(...)

load_dataset_types(...)

download_file(...)

year_filter(...)

data_deliverer_filter(...)

read_data(...)

validate_dataset_names(...)

load_dyntaxa_taxonomy(...)

read_species_list(...)

gather_species_info(...)

add_species_info(...)

load_worms_taxonomy(...)

gather_worms_species_info(...)

add_worms_species_info(...)

get_geographical_info(...)

filter_outdated_datasets(...)
```

## Arguments

- ...:

  Arguments previously accepted by the function. Ignored.
